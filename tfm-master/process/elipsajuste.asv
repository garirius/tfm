function [s,r1,r2] = elipsajuste(A,B)
%Realiza un ajuste de una figura de Lissajous dados los puntos A,B

if(size(A)==size(B))
    m = size(A,2);
    
else
    'Las matrices no comparten tamaño.'
    s = NaN;
    r1 = NaN;
    r2 = NaN;
end

end

